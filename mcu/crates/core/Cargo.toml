[package]
name = "struckout-mcu-core"
version = "0.1.0"
edition = { workspace = true }

[dependencies]
heapless = { workspace = true }
bt-hci = { workspace = true }

embassy-sync = { workspace = true }
embassy-futures = { workspace = true }

[features]
defmt = []
log = []
