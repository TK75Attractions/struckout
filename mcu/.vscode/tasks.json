{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-release",
            "command": ". ~/export-esp.sh&&cargo build --release",
            "type": "shell"
        }
    ]
}